import { DatetimeToRelativePipe } from './datetime-to-relative.pipe';

describe('DatetimeToRelativePipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeToRelativePipe();
    expect(pipe).toBeTruthy();
  });
});
