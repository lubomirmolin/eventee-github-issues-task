import { DatetimeToFormatPipe } from './datetime-to-format.pipe';

describe('DatetimeToFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeToFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
